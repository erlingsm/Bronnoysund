apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bronnoysund-lookup.fullname" . }}-config
  labels:
    {{- include "bronnoysund-lookup.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
