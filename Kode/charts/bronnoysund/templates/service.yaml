apiVersion: v1
kind: Service
metadata:
  name: {{ include "bronnoysund.fullname" . }}
  labels:
    {{- include "bronnoysund.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bronnoysund.selectorLabels" . | nindent 4 }}
