apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bronnoysund.fullname" . }}-config
  labels:
    {{- include "bronnoysund.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
